<Window x:Class="TranslateText.UI.TranslateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Translating..."
        Width="440" Height="180"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/TranslateText;component/UI/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="10" Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource TitleBarBrush}"
                    CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <DockPanel Margin="12,0">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                        <Border Width="22" Height="22" CornerRadius="4"
                                Background="{StaticResource AccentBrush}" VerticalAlignment="Center">
                            <Image x:Name="imgIcon" Width="18" Height="18"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Translate Text" Foreground="{StaticResource FgBrush}"
                                   FontFamily="Segoe UI" FontSize="13"
                                   FontWeight="SemiBold" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <Button x:Name="btnClose" Content="✕" DockPanel.Dock="Right"
                            HorizontalAlignment="Right" Width="32" Height="26" Padding="0"
                            Style="{StaticResource CloseButton}" Click="BtnCancel_Click"/>
                </DockPanel>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="16,12,16,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="tbStatus" Grid.Row="0"
                           Style="{StaticResource FormLabel}"
                           Text="Preparing..."/>
                <ProgressBar x:Name="progressBar" Grid.Row="2"
                             Style="{StaticResource DarkProgressBar}"
                             Minimum="0" Maximum="100"/>
            </Grid>

            <!-- Bottom Bar -->
            <Border Grid.Row="2" Background="{StaticResource TitleBarBrush}" CornerRadius="0,0,10,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center" Margin="14,0">
                    <Button x:Name="btnCancel" Content="Cancel" Width="120" Height="34"
                            Style="{StaticResource DangerButton}"
                            Click="BtnCancel_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>