<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="TranslateText.UI.TranslateWindow"
        Title="Translate Text"
        Width="440" Height="270"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/TranslateText;component/UI/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="10" Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource TitleBarBrush}"
                    CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <DockPanel Margin="12,0">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                        <Border Width="22" Height="22" CornerRadius="4"
                                Background="{StaticResource AccentBrush}" VerticalAlignment="Center">
                            <Image x:Name="imgIcon" Width="18" Height="18"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Translate Text" Foreground="{StaticResource FgBrush}"
                                   FontFamily="Segoe UI" FontSize="13"
                                   FontWeight="SemiBold" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <Button x:Name="btnClose" Content="✕" DockPanel.Dock="Right"
                            HorizontalAlignment="Right" Width="32" Height="26" Padding="0"
                            Style="{StaticResource CloseButton}" Click="BtnClose_Click"/>
                </DockPanel>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="16,12,16,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Source Language -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Source Lang:"
                           Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="cbSource" Grid.Row="0" Grid.Column="1"
                          Style="{StaticResource DarkComboBox}"
                          ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>

                <!-- Target Language -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Target Lang:"
                           Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="cbTarget" Grid.Row="2" Grid.Column="1"
                          Style="{StaticResource DarkComboBox}"
                          ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>

                <!-- Text Style -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Text Style:"
                           Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="cbStyle" Grid.Row="4" Grid.Column="1"
                          Style="{StaticResource DarkComboBox}"
                          ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>

                <!-- Text Case -->
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Text Case:"
                           Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="cbTextCase" Grid.Row="6" Grid.Column="1"
                          Style="{StaticResource DarkComboBox}"
                          ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>
            </Grid>

            <!-- Bottom Bar -->
            <Border Grid.Row="2" Background="{StaticResource TitleBarBrush}" CornerRadius="0,0,10,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                            VerticalAlignment="Center" Margin="14,0">
                    <Button x:Name="btnCancel" Content="Cancel" Width="90" Height="34"
                            Margin="0,0,8,0" Style="{StaticResource DarkButton}"
                            Click="BtnCancel_Click"/>
                    <Button x:Name="btnTranslate" Content="Translate" Width="120" Height="34"
                            Style="{StaticResource AccentButton}"
                            Click="BtnTranslate_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>